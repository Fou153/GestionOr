workflows:
  android-workflow:
    name: Android Build

    environment:
      vars:
        PACKAGE_NAME: "com.fou.gestionor"

    scripts:
      - name: Installer les dépendances système (macOS)
        script: |
          brew update
          brew install autoconf libtool pkg-config
          
      - name: Installer Buildozer et Cython
        script: |
          pip3 install buildozer cython

      - name: Accepter automatiquement les licences Android SDK
        script: |
          mkdir -p "$HOME/.android"
          touch "$HOME/.android/repositories.cfg"
          
          # Option 1 : Accepter via le sdkmanager natif de la machine Codemagic
          if command -v sdkmanager &> /dev/null; then
            yes | sdkmanager --licenses
          fi

      - name: Compiler l'application en APK (Debug)
        script: |
          # On passe l'argument automatique directement à Buildozer pendant le build
          yes | buildozer android debug

    artifacts:
      - bin/*.apk
