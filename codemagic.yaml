workflows:
  android-workflow:
    name: Android Build

    environment:
      vars:
        PACKAGE_NAME: "com.fou.gestionor"

    scripts:
      - pip install buildozer
      - buildozer android debug

    artifacts:
      - bin/*.apk
